<div>
  <% if @event.cover_img.attached? %>
    <div class="banner-event" style="background-image:linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), 
  url(<%= cl_image_path(@event.cover_img.key) %>);">
  <div class="container">
      <%= render 'shared/avatar' %>
  </div>
  <% else %>
    <div class="banner-event">
    </div>
  <% end %>
</div>
